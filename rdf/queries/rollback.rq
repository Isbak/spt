PREFIX exec: <https://example.org/semantic-platform/execution#>
SELECT ?action ?plan ?supported ?description WHERE {
  ?action exec:hasRollbackPlan ?plan .
  ?plan exec:rollbackSupported ?supported ; exec:rollbackDescription ?description .
}
